#include "main.h"

/**
 * _abs - checks if character is in lower case
 *
 * Description: 'the program's description'
 *
 * @n: the inputed character
 * Return: Always 1 or 0 (Success)
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (n * -1);
	}
	return (n);
}
